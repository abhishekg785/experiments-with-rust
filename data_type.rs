//rust is a static typed language i.e checks data type at compile time
// let is used to bind values to variables
fn main(){
	let a = true;
	let b:bool = true;

	let (x,y) = (1,2);      
	  
	let mut z = 5;
	z = 6;
}
